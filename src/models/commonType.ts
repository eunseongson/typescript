export interface IExternalUrls {
    spotify: string;
}

export interface IImages {
    url: string;
    height: number | null;
    width: number | null;
}

export interface IRestrictions {
    reason: string;
}